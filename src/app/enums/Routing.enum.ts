export const enum RoutingEnum {
  About = 'about',
  Blog = 'blog',
  BlogPost = 'blog/:slug',
  Services = 'services',
  Work = 'work',
  Certs = 'certs',
  CertsSlug = 'certs/:slug',
  Certificates = 'certificates',
  CertificatesSlug = 'certificates/:slug',
  NotFound = '**'
}
